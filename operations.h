#ifndef OPERATIONS_H
#define OPERATIONS_H

enum FUNCTION_TYPE {
  SOURCE_DC,
  SOURCE_PULSE,
  SOURCE_SWEEP,
  DIGITIZE,
  GRAPH
};

#define SET  200
#define LIMIT  201

enum OPERATION_TYPE {
  SOURCE_VOLTAGE,
  SOURCE_CURRENT
};

enum CURRENT_RANGE {
  AMP1,
  MILLIAMP10
};


#endif
