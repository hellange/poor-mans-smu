#ifndef OPERATIONS_H
#define OPERATIONS_H


#define _SOURCE_AND_SINK 111


enum FUNCTION_TYPE {
  SOURCE_DC_VOLTAGE,
  SOURCE_DC_CURRENT,
  SOURCE_PULSE,
  SOURCE_SWEEP,
  DIGITIZE,
  GRAPH,
  DATALOGGER,
  MEASURE_RESISTANCE,
  SHOWSETTINGS
};

#define SET  200
#define LIMIT  201

enum OPERATION_TYPE {
  SOURCE_VOLTAGE,
  SOURCE_CURRENT
};

enum CURRENT_RANGE {
  AMP1,
  MILLIAMP10
};


#endif
